node_modules/
dist/
*.tsbuildinfo
*.log
.DS_Store
.env

# upstream n8n clone kept locally as an implementation reference
n8n/

# local opencode config with secrets (MCP tokens)
opencode.json

# local git worktrees for isolated feature work
.worktrees/
